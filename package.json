{
  "name": "gemini-schedule-demo",
  "version": "1.0.0",
  "description": "A demo application showing how to use Google's Gemini API to create student schedules",
  "main": "dist/anchor-assist-tests.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/anchor-assist-tests.js",
    "dev": "ts-node --transpile-only anchor-assist-tests.ts",
    "test": "npm run build && node dist/anchor-assist-tests.js",
    "dayplanner": "npm run build && node dist/dayplanner-tests.js"
  },
  "keywords": ["gemini", "ai", "schedule", "student", "demo", "typescript"],
  "author": "Student Demo",
  "license": "MIT",
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  }
}
